import styled from '@emotion/styled';
import { Col, Button, Row, Typography, Table, Input } from 'antd';

export const PackServRow = styled(Row)`
padding:10px;
    @media (max-width: 576px) {
        // flex-direction: column;
        wrap : nowrap
        // max-width: 100%;
        // margin: 31px;
        gap: 20px !important;
        height: auto !important;
    }
    .BookingImage{
       
    }

`;